RNN LSTM based poem generator.
It is a characrer based model and struggles to generate sensible words in most cases. 
Instead of encoding characters to intigers, encoding words to integers would generate better phrases with sensible words.
